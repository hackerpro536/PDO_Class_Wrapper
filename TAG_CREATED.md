# Git Tag Created

A git tag `v1.3` has been created for the PDO Class Wrapper project.

## Tag Details
- **Tag Name**: v1.3
- **Tag Type**: Annotated tag
- **Message**: Release version 1.3 - PHP PDO Class Wrapper with PHP 8.x support

## How to Push the Tag

The tag has been created locally. To push it to the remote repository, use:

```bash
git push origin v1.3
```

Or to push all tags:

```bash
git push --tags
```

## Verify Tag

To verify the tag exists:

```bash
git tag -l
git show v1.3
```
